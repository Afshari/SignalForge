syntax = "proto3";

package SignalForge;

service SignalForgeService {
    rpc SendFile (FileRequest) returns (FileResponse);
}

message FileRequest {
    bytes data = 1;
}

message FileResponse {
    bool   accepted = 1;
    string message  = 2;
}